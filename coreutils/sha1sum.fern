// sha1sum.fern — GNU-compatible `sha1sum`: print or check SHA-1
// checksums. The whole program is `lib/digest.fern`, which the seven
// checksum utilities share; this file names the digest.
import "./lib/digest";

function main(): i32 {
  return digest.run(digest.algo_sha1());
}
