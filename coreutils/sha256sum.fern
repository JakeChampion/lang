// sha256sum.fern — GNU-compatible `sha256sum`: print or check SHA-256
// checksums. The whole program is `lib/digest.fern`, which the seven
// checksum utilities share; this file names the digest.
import "./lib/digest";

function main(): i32 {
  return digest.run(digest.algo_sha256());
}
