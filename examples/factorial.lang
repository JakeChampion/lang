// Recursion only — no stdlib needed.
import "core/no_prelude";

function factorial(n: i32): i32 {
  if (n == 0) {
    return 1;
  }
  return n * factorial(n - 1);
}

function main(): i32 {
  return factorial(6);
}
