Reports the SHAPE of the allocator's fresh-byte curve over a read_chunk
loop: "constant" (a repeat round costs nothing) with reclamation on,
"scales" with it off. The *FixturesFreeMatchesNoFree gates therefore
require the two runs to DIFFER; see alloc_flat_under_reclaim/reclaim-observable
for the contract.
