// Evaluate the return value before cleanup. Rebinding a record's source in a
// defer does not replace the value the caller receives.
struct P { a: i32 }

function build(): P {
    var p: P = P { a: 3 };
    defer p = P { a: 9 };
    return p;
}

function main(): i32 {
    return build().a;
}
