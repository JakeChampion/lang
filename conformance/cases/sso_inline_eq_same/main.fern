function main(): i32 {
    var a: str = slice_unchecked("abcdef", 0, 3); // inline "abc"
    var b: str = slice_unchecked("xabc", 1, 4);    // inline "abc"
    if (a == b) { return 1; }
    return 0;
}
