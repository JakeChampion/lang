// Pins E006. The program is the example from that code's entry
// in the diagnostic catalogue (`fern explain E006`), so this case
// also checks the catalogue's example really does produce the error
// it documents.
struct Point { x: i32, y: i32 }
struct Point { x: f32, y: f32 }   // error[E006]: struct "Point" redeclared
