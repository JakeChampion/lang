waiver: harness-limit
reason: internal/interp has no bump allocator — __heap_bump_bytes() returns
  0 there unconditionally and the shape this case measures is unobservable
  (see alloc_flat_under_reclaim and docs/ALLOCATION-OBSERVABLE.md).
