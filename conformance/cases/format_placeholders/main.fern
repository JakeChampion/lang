import "std/format";
// format.format() substitutes positional `{}` placeholders from a string[].
function main(): i32 {
    print(format.format("{} + {} = {}", [(40).to_string(), (2).to_string(), (42).to_string()]));
    return 0;
}
