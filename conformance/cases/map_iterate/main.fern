import "core/map";
// `for (k, v) in m` cursor iteration over a Map literal.
// (1+10) + (2+20) + (3+12) = 48.
function main(): i32 {
    var m: Map[i32, i32] = Map { 1: 10, 2: 20, 3: 12 };
    var total: i32 = 0;
    for (k, v) in m {
        total = total + k + v;
    }
    return total;
}
