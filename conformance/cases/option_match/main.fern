// Built-in Option[T]: None on the zero-divisor path, Some otherwise.
function div(a: i32, b: i32): Option[i32] {
    if (b == 0) { return None; }
    return Some(a / b);
}

function main(): i32 {
    match (div(84, 2)) {
        Some(n) => { return n; },
        None => { return 255; }
    }
}
