import "std/string";
// String prelude methods: to_upper, len, contains.
function main(): i32 {
    var s: string = "Hello, Fern";
    print(s.to_upper());
    print(s.len().to_string());
    if (s.contains("Fern")) {
        print("has-fern");
    }
    return 0;
}
