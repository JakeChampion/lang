import "core/map";
import "std/array";
// Map.keys() snapshots keys into an array; sum them. 10+20+12 = 42.
function main(): i32 {
    var m: Map[i32, i32] = Map { 10: 1, 20: 2, 12: 3 };
    return m.keys().sum();
}
