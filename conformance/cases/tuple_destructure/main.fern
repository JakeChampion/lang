// A function returning a tuple, unpacked with a destructuring var.
function swap(a: i32, b: i32): (i32, i32) {
    return (b, a);
}

function main(): i32 {
    var (x, y) = swap(10, 32);
    return x + y;
}
