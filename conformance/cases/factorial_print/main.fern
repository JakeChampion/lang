import "std/i32";
// 10! = 3628800, printed (exceeds the exit-code range).
function fact(n: i32): i32 {
    if (n <= 1) { return 1; }
    return n * fact(n - 1);
}

function main(): i32 {
    print(fact(10).to_string());
    return 0;
}
