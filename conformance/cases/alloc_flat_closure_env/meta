waiver: harness-limit
reason: internal/interp has no bump allocator, so __heap_bump_bytes()
  returns 0 there unconditionally and the shape this case measures is
  unobservable — see docs/ALLOCATION-OBSERVABLE.md.
