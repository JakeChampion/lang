{
  "$comment": [
    "The heavy CI lanes the admission queue serialises. One PR's worth of these",
    "runs at a time — see docs/CI-QUEUE.md. `file` is the workflow filename (the",
    "id createWorkflowDispatch takes); `name` is its `name:` (what the",
    "workflow_run trigger in ci-queue.yml and pr-status-comment.yml match on).",
    "",
    "Deliberately NOT lanes:",
    "  lint.yml            — the fast lane. Runs immediately on every PR so a",
    "                        typo doesn't wait behind a 90-job matrix.",
    "  docs-build.yml, macos.yml, playground-e2e.yml, vscode-extension.yml —",
    "                        path-filtered, so they already only fire on the",
    "                        changes that need them. `paths:` filters do not",
    "                        apply to workflow_dispatch, so queueing them would",
    "                        make them run on EVERY front-of-queue PR: strictly",
    "                        more CI, not less.",
    "  cancel-on-merge.yml, pr-status-comment.yml, ci-queue.yml — plumbing.",
    "  pages.yml, release.yml — not PR-triggered.",
    "",
    "internal/sourcelint's TestCIQueue* keep this list in sync with the",
    "workflow files."
  ],
  "lanes": [
    { "file": "examples.yml", "name": "Examples" },
    { "file": "fuzz-diff.yml", "name": "Fuzz differential execution" },
    { "file": "fuzz-parse.yml", "name": "Fuzz parse round-trip" },
    { "file": "test-e2e-arm64.yml", "name": "Test e2e arm64" },
    { "file": "test-e2e-differential.yml", "name": "Test e2e differential" },
    { "file": "test-e2e-other.yml", "name": "Test e2e other" },
    { "file": "test-e2e-selfhost.yml", "name": "Test e2e self-host" },
    { "file": "test-e2e-wasm.yml", "name": "Test e2e wasm" },
    { "file": "test-e2e-x86_64.yml", "name": "Test e2e x86_64" },
    { "file": "test-fernsmith.yml", "name": "Test fernsmith" },
    { "file": "test-units.yml", "name": "Test units" }
  ]
}
