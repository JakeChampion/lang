# Retired- and static-instruction baselines for the examples/bench corpus,
# checked against every run by scripts/ci-check-perf.
#
# FORMAT: metric  value  [tolerance%]. The metric key is
# <arch>/<benchmark>.<text|ir> — .text is the count of instructions in the
# emitted assembly, .ir the count of instructions actually retired.
#
# WHAT IS EXACT AND WHAT IS NOT. Every figure here repeats to the digit on a
# given commit EXCEPT `map_string.ir`: core/map draws a per-process
# string-hash seed (#6194), which moves probe lengths. Measured spread over
# five runs: 291,105,501 .. 293,300,059, i.e. 0.75%. That one line therefore
# carries an explicit 2% tolerance and is baselined at the middle of the
# range rather than at a sample. `map_int.ir` is exact — the seed is consulted
# for string keys only — and is deliberately left on the default so the two
# together show which half of a map regression moved.
#
# Everything else uses the 1% default, which flags a change of one part in a
# hundred. That is a significance threshold, not a noise floor: the numbers
# have no noise.
#
# ADVISORY, IN BOTH DIRECTIONS — see scripts/ci-check-perf. When a number
# moves on purpose, update it in the same PR; the job summary prints the
# corrected lines ready to paste. Keep the reason in the PR, not here.
#
# TWO ARCHITECTURES. An instruction count is not comparable across ISAs, so the
# keys are namespaced by arch and compared independently — and each job compares
# only its OWN namespace, so an x86_64 entry is not a missing measurement on the
# aarch64 runner.

x86_64/array_append.text	618
x86_64/array_append.ir	97597508
x86_64/array_index.text	582
x86_64/array_index.ir	100303812
x86_64/call_overhead.text	211
x86_64/call_overhead.ir	63101700
x86_64/closure_call.text	491
x86_64/closure_call.ir	284520055
x86_64/enum_match.text	820
x86_64/enum_match.ir	261000071
x86_64/int_loop.text	121
x86_64/int_loop.ir	84000036
x86_64/map_int.text	4028
x86_64/map_int.ir	136339727
x86_64/map_string.text	5346
x86_64/map_string.ir	292500000	2
x86_64/sort_inplace.text	1001
x86_64/sort_inplace.ir	201915356
x86_64/sort_ints.text	3321
x86_64/sort_ints.ir	157651167
x86_64/string_build.text	1467
x86_64/string_build.ir	199336343
x86_64/string_scan.text	1841
x86_64/string_scan.ir	140977346
x86_64/string_slice.text	511
x86_64/string_slice.ir	196020101
x86_64/struct_drop.text	2348
x86_64/struct_drop.ir	682100167
x86_64/tokenize.text	997
x86_64/tokenize.ir	207560093

# aarch64, from the Perf workflow's own perf-aarch64 job — the arch this
# project defaults to. Seeded from CI rather than measured here, because no
# aarch64 host is available to the dev container; the figures are the job's
# paste block verbatim (run 31947059512 for the original nine, 31964295961 for
# closure_call / enum_match / sort_inplace / sort_ints / string_build /
# tokenize). map_string.ir carries the same 2% the x86_64 twin does, for the
# same per-process hash seed.
#
# Do NOT extrapolate an aarch64 figure from its x86_64 twin: the ratio is not
# constant (int_loop 0.71, string_scan 1.01). A new benchmark reports as
# UNBASELINED here for one CI run, which is the gate saying "measured,
# recorded, not yet compared" — paste that run's block in rather than guessing.
aarch64/array_append.text	539
aarch64/array_append.ir	71008054
aarch64/array_index.text	510
aarch64/array_index.ir	63698033
aarch64/call_overhead.text	165
aarch64/call_overhead.ir	45601201
aarch64/closure_call.text	444
aarch64/closure_call.ir	209928045
aarch64/enum_match.text	682
aarch64/enum_match.ir	194175068
aarch64/int_loop.text	103
aarch64/int_loop.ir	60000026
aarch64/map_int.text	3095
aarch64/map_int.ir	100592487
aarch64/map_string.text	4171
aarch64/map_string.ir	223648093	2
aarch64/sort_inplace.text	815
aarch64/sort_inplace.ir	132740196
aarch64/sort_ints.text	2631
aarch64/sort_ints.ir	115263483
aarch64/string_build.text	1214
aarch64/string_build.ir	242431740
aarch64/string_scan.text	1543
aarch64/string_scan.ir	142229869
aarch64/string_slice.text	497
aarch64/string_slice.ir	167280114
aarch64/struct_drop.text	1864
aarch64/struct_drop.ir	692820288
aarch64/tokenize.text	944
aarch64/tokenize.ir	176490114
