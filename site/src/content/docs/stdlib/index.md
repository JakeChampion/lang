---
title: Standard library
description: Auto-generated reference for every std/* module.
sidebar:
  order: 0
---

The pages under this section are generated by `cmd/ferndoc`,
which parses `internal/stdlib/*.fern` and emits one Markdown
file per module with the public functions, structs, enums and
constants — along with any doc comments preceding each
declaration.

Run the generator manually with:

```bash
go run ./cmd/ferndoc -out site/src/content/docs/stdlib/
```

It runs automatically as part of `npm run build` in `site/`.

## Modules

Browse the sidebar to the left, or peek at the source under
[`internal/stdlib/std/`][1] on GitHub.

[1]: https://github.com/JakeChampion/lang/tree/main/internal/stdlib/std
