{
  "name": "lang-docs-playwright",
  "version": "0.0.0",
  "description": "Scripted browser regressions for the lang docs site.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
