# Build artefacts produced by web/build.sh — not source.
# A static host (e.g. GitHub Pages) regenerates these on push;
# committing the binary would balloon the repo by ~28 MB per
# version of the toolchain (6 MB gzipped, which is what is served).
fern.wasm
fern.wasm.gz
