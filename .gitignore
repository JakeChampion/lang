/fern
/fern-lsp
/ferndoc
/fern-wasm
/dump_arm64
/lang
/lang-lsp
/bin/
/build/
/.gotmp/
# scripts/netlify-smoke-test's local cache (self-host driver binaries, the
# pinned wasm toolchain). On Netlify this lives in the build cache instead.
/.smoke-cache/
*.s
*.o
# Ignore local Claude Code config, but track the shared SessionStart hook
# + settings so Claude Code on the web provisions the test toolchain.
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
/unicodegen
