# Seeds whose SELF-HOST x86-64 result disagrees with the interpreter's, for
# TestDifferential_SelfHostX86_64 (internal/e2e/diff_oracle_selfhost_test.go).
#
# Format: <seed> <reason + issue>. Blank lines and # comments ignored.
#
# A row is a KNOWN COMPILER BUG being tolerated so the rest of the corpus keeps
# running — not a seed that is allowed to be wrong. The leg fails on a NEW
# divergence and equally on a LISTED seed that starts agreeing, so this list
# prunes itself as the bugs are fixed rather than becoming somewhere they are
# forgotten.
#
# For every row here the interpreter, native x86-64 and native arm64 agree with
# each other; that agreement is what says the self-host compiler is wrong rather
# than the generated program being invalid.
#
# THE LIST IS EMPTY. Every seed the leg can compile now agrees with the
# interpreter. Keep the file — an empty list is the strongest form of the gate,
# since any divergence at all fails the leg — and keep the record below of what
# left, because it is what the next round of probing should be measured against.
#
# Measured 2026-08-20 over seeds 0..511 (the leg's default corpus): 511 agree,
# 0 diverge, 1 compile gap — the `<fn>$clo not defined` class (#6256), so every
# other category in the census below is empty.
#
# This is the only place the figure is derived. Quote it from here and re-sweep
# before you do: a count carried into an issue body or a doc is stale within
# days at this cadence, which is what retired the parity census in #6645.
#
# Count the gaps, not just the agreements, when reading a run: the leg only
# proves what it could COMPILE, so a fix that turns a wrong answer into an IR
# bail raises the gap count while looking like a win in the divergence column.
#
# That caution cuts both ways, and every run since has gone the other way: 204
# agree / 308 gaps, then 263 / 249 (#6237 + #6246), 291 / 221 (#6261), 311 / 201
# (#6268), 325 / 187 (#6272), 338 / 174 (#6276), 361 / 151, 504 / 8, 507 / 5,
# 508 / 4, 509 / 3, 511 / 1.
# One bail can cost far more than the seed that exposes it — an if-expression
# whose branches are 64-bit took the whole MODULE off the IR path, and fernsmith
# emits if-expressions everywhere, so a single missing branch in one classifier
# was suppressing a tenth of the corpus.
#
# HOW TO READ A BAIL: just run it. `FERN_STRICT_IR=1` now names the innermost AST
# node that refused, via LowerState.fail_why —
#
#   FERN_STRICT_IR: gen_f0 (did not lower: `return` of binary `|`)
#
# This replaces a gdb recipe that used to live here (build with -g, break on
# LowerState.fail, read the caller's line). That recipe worked and is how the
# census below was first built, but it is now strictly worse than reading the
# message, so it has been deleted rather than left as a second way to do the
# same thing.
#
# CENSUS OF THE GAPS, so the next round is aimed rather than sampled. Every
# remaining gap is the `<fn>$clo not defined` class (#6256); the
# `T.to_string`-on-a-bare-type-var and named-fail_why categories that used to sit
# beside it are empty.
#
# The class is NOT one mechanism, which is the standing lesson here: grouping by
# message is what made the first reduction fix the wrong thing. #6256's sweep
# separated the survivors by instrumenting `lower_expr`'s `ExprLambda` arm. The
# fn-valued-capture row (s0356, s0172) left with #6861; one remains:
#
#   1  nested IIFEs in non-array fn-value position                (s0199)
#
# The array-gate pair left together, and neither was the passthrough ARM the
# census guessed at. s0211 was a capture-SCOPE miss: a value-position if/match
# binds names of its own — a match arm's payload — and the array gate read
# captures against the enclosing function alone, so `Ok(a) => [(x) => x + a]`
# looked capture-free, the gate answered 0 and the arm lambda stayed raw. The
# non-array twin and iife_arm_lambda_captures already scope through
# iife_scope_fd. s0042 was an arm spelled `id([…])`: a passthrough hands the
# literal straight back, but the gate demanded a literal and abandoned the whole
# rewrite. Destination first there as always — expr_is_closure_array sees
# through the same passthrough now, so the binding reads as a closure array
# instead of bare-dispatching a box.
#
# s0215 left when the passthrough rule reached both sides: the boxing pass now
# descends a passthrough nested in another, and the closure-ARRAY registry
# recognises a passthrough element (and a returned value-position if/match).
# That ordering was load-bearing — widening the boxing alone turns safe bails
# into SIGSEGVs, because a destination that does not read the element as a box
# bare-dispatches one. `expr_is_closure_box`, the non-array arm-value twin, still
# has no `PASSTHRU:` branch, so the same hazard stands for whatever needs it next.
#
# s0393 left with the capture side, and it was a decline rather than a miss:
# `cap_param_for` builds a hoisted IIFE's capture parameter only from an EXACT
# signature, and `var v0: (i32) => i32 = pick(…)` is neither a lambda initialiser
# nor a `__mkclo$` target. The signature was not unrecoverable, it was DROPPED —
# the parser read the annotation's return into `v_fn_ret` and then kept only the
# coarse "fn" tag on the StmtVar. `StmtVar.fn_ret` now carries it, and paired
# with the annotated params of the lambda the passthrough forwards that is a
# complete signature with nothing inferred from a body, which is the standard
# this predicate has to meet (wasm funcref types are structural, so a wrong one
# is a module the loader rejects rather than a slow path).
#
# Read a moved count as a reordering before a regression: this class grew
# 71 → 78 → 90 while nothing about it changed, purely because seeds that used to
# stop earlier began reaching it. Re-measure before picking — draining one pile
# reshuffles the others.

# CRASHES — the self-host-compiled binary died by SIGSEGV where the interpreter
# returned a value. These were ONE bug: #6207, a `Map { … }` literal whose keys
# the parser could not classify, building itself string-keyed so map_set
# compared integer keys through __fern_str_eq. Eight rows left with it —
# 200 / 228 / 455 with the two classifiable key forms (#6213 leaf-reachable
# arithmetic, #6220 cast targets), then 59 / 97 / 144 once the fix stopped being
# syntactic and read the `Map[K, V]` declaration instead. Every crash row in the
# corpus was that bug; the header used to hedge that the survivors might be "a
# different cause (or causes)", which was true of what had been measured at the
# time and false once they were reduced.

# WRONG VALUES + ARENA EXHAUSTION — both sections emptied together, because they
# were ONE bug. infer_expr_width classified a number literal by MAGNITUDE alone,
# so a small suffixed literal (`7i64`) read as 32 bits and every 64-bit value
# derived from one was computed at the wrong width. Fifteen rows left with that
# fix: 4, 20, 28, 29, 48, 52, 99, 117, 123, 241, 324, 362, 364, 375, 482. The
# old note here — that the interp=0 rows were "worth checking as a group before
# reducing them one at a time" — was right; 117's "arena exhausted" was a
# symptom of the same mis-widening, not a separate leak.
#
# Seed 502 left in two steps. #6228 first moved it from a silent wrong answer to
# an IR bail (`FERN_STRICT_IR: __lam_22`) — the safe direction, but a gap rather
# than a fix — and #6237 then closed the bail, so it now compiles and agrees.
#
# The cause was NOT irt_guess, which this note used to name and which cost an
# hour to rule out. A lifted if-EXPRESSION gets its return type from
# `if_expr_rt` at PARSE time, and because that writes a non-EMPTY "i32",
# infer_ret_types_module never revisits it — so irt_guess never runs on these
# lambdas at all. Teaching irt_guess the suffix AND the magnitude changes
# nothing; measured, twice. `if_expr_rt` was the fourth literal classifier in
# parser.fern and the last one that read neither.
#
# The hole PREDATED the width work either way: a big-magnitude branch
# (`if (c) { 5000000000 } else { … }`) bailed identically before #6228, with no
# suffix involved.

# Also gone earlier, with their own fixes: 121 / 265 / 365 (#6177, the maps
# runtime need-gate).
