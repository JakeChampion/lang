// The Ackermann function — deep, non-tail, doubly-recursive.
// A(2, 3) = 9.
function ack(m: i32, n: i32): i32 {
    if (m == 0) { return n + 1; }
    if (n == 0) { return ack(m - 1, 1); }
    return ack(m - 1, ack(m, n - 1));
}

function main(): i32 {
    return ack(2, 3);
}
