// Short-circuit && / ||, unary !, and parenthesised grouping.
function main(): i32 {
    var t: boolean = true;
    var f: boolean = false;
    if (t && !f && (t || f)) {
        return 42;
    }
    return 0;
}
