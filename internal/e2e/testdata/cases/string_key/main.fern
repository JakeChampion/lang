import "core/map";
function main(): i32 {
    var m: Map[string, i32] = map_new(4);
    m = m.insert("hello", 42);
    match (m.get("hello")) {
        Some(v) => { return v; },
        None => { return 0; }
    }
    return 1;
}
