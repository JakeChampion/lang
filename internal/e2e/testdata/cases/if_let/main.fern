// `if let` pattern binding with an else arm.
function main(): i32 {
    var m: Map[i32, i32] = map_new(4);
    m.set(7, 42);
    if let Some(v) = m.get(7) {
        return v;
    } else {
        return 1;
    }
}
