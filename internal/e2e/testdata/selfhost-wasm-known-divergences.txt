# Fixtures where the SELF-HOST wasm path disagrees with the native compiler.
#
# Format: <fixture-name> <#issue> <reason>
#
# TestFernFixturesSelfHostWasm lists these rather than skipping them, following
# tools/deadcode-allowlist.txt: the lane is green with them listed, a NEW
# divergence fails, and a listed fixture that starts PASSING also fails — an
# allowlist nobody prunes is where bugs go to be forgotten.
#
# `eq_struct_enum` and `ord_struct_enum` were here and are now FIXED (#6009):
# the self-host had no composite-comparison desugar at all, so `a == b` and
# `a < b` on a struct or enum lowered the two BOX POINTERS into a plain i32
# compare — structural equality answered "are these the same allocation" and
# ordering answered "which was allocated first". irlower now rewrites both to
# the type's derived `eq` / `cmp` the way the native checker does. The operator
# forms in self_host_derive_{eq,ord}_ir_test.go pin the shape directly.
#
# `audit_numeric_types` was here and is now FIXED (#6010): the self-host IR path
# applied its zero-cost-ascription rewrite (`var x: T = E as T` -> `var x: T = E`)
# to a real width conversion, so a narrowing `as u8` lost its mask in BINDING
# position only — 260 as u8 stayed 260 and the fixture's u8-wrap check returned 13.
# `cast_narrow_binding` now pins that shape directly.
#
# `tuple_match_expr` was here and is now FIXED (#6010): a tuple- or
# struct-pattern match in EXPRESSION position desugars into a done-flag if-chain
# whose arm bodies still ended in `return`, and a capturing IIFE lowers INLINE —
# so the arm returned from the ENCLOSING function and everything after the match
# was dead code. The arms now assign to a value local instead.
#
# `pure_collection_aliases` was here and is now FIXED (the #6007 slice): the
# self-host had no `Map.cleared` lowering at all — the name was known to the
# checker but nothing lowered it, so the module bailed on an unknown
# `Map.cleared` symbol. It is now construction: this runtime's map box is
# [keys, values] with no capacity field, so a fresh empty box of the same key
# kind IS a cleared map.
#
# `map_struct_value_field` was here and is now FIXED (the #6007 slice): its own
# header blames `get_or(..).field` on a Map[K, Struct], but that half always
# lowered — the bail was `it.value().field`, where expr_struct_type had a struct
# recovery for the get_or receiver and none for the iterator's. Both read the
# same V.
#
# `derive_json` was here and is now FIXED, in two halves. (1) Every @derive over
# an enum VARIANT bound a single `__p0`, so `Rect(2, 3)` serialised, compared,
# ordered and hashed as `Rect(2)` — derived `eq` answered TRUE for
# `Rect(2, 3) == Rect(2, 9)`; pinned by `derive_multi_payload`. (2) With that
# gone, `.to_json()` on an `i32[]` FIELD receiver was seen to render the array
# POINTER: the guard listing the array predicates relies on expr_is_arr_src for
# scalar arrays, and that means array SOURCE (fresh/owned, an rc concept), not
# "is an array", so a field READ fell through to the primitive path. Pinned by
# `json_array_field`.
#
# `regex_captures`, `regex_captures_all`, `regex_named_groups` and
# `regex_replace_groups` were here and are now FIXED (#6008). All four trapped
# on `?` over a CAPTURING group, and the fault was neither in the capture
# machinery nor in the wasm backend: irlower bound `var pg = prog.append(v)` —
# std/regex's __rx_emit — straight to the buffer arr_push had appended into IN
# PLACE, so a frame and its caller each held one buffer under two names and each
# released it. The freed cell was recycled by the next `caps.with(slot, ti)`, so
# the Pike VM read an ISplit's targets out of a capture-slot array and branched
# to prog[-1]. The same double free was latent on x86-64 and arm64 — visible
# under a poison-on-free arr_dec, invisible without one, because nothing
# recycled the cell there in time. irlower now brackets such a receiver so
# arr_push takes its copy path.
#
# NONE of the original twelve remains — #6008 took the last four and #6007
# the row below it, so this leg's list is empty too, like its x86-64 and
# arm64 siblings. Keep the contract in force rather than deleting the file:
# an empty allowlist is what makes the NEXT divergence fail the leg.
#
# All twelve were found by the leg's first run
# over the existing corpus, on fixtures that had been green for months because
# all four of TestFernFixtures' backends are the native compiler. Each has been reproduced by hand with a
# native self-host CLI (`make selfhost-cli`) before being written down; none is
# a harness artifact. The first pass reported 26 mismatches and 14 of those WERE
# artifacts — WASI caps exit status at 125, so nine regex fixtures whose expected
# 255 is a feature bitmask looked like a broken regex engine — which is why every
# entry here names a measured before/after rather than a symptom.

# `generic_method_typeparams` was here and is now FIXED (#6007): a method on a
# generic struct that declares type params of its OWN (`(b: Box[T]) map[U]`)
# was cloned per RECEIVER instantiation with its own params dropped, so `U`
# survived free into the clone and the return `Box[U]` mangled to a `Box__U`
# no pass ever generated. Such a method now folds into a free generic
# `__smm_<Base>_<name>[<receiver vars>, <own vars>]` and rides the same
# worklist the array (`__arrm_`) and map (`__mapm_`) folds do — so its clone
# carries a CONCRETE `Box[i32]` / `Box[boolean]` return for the struct pass.
# The last IR-ineligible row on this leg went with it.

# --- UNMEASURABLE by this oracle ------------------------------------------
# Not listed as divergences because the leg cannot prove them either way: an
# expected exit >= 126 is refused by WASI, so wasmtime reports 1 and the real
# answer is invisible. `regex_captures_assert` (expected 255) and `regex_groups`
# (expected 127) read 47 and 111 under #6008 and now read 255 and 127 — measured
# on the self-host x86-64 leg, which has no exit cap, since this one still
# cannot see them.

# `alloc_flat_fresh_array_arg` was here and is now FIXED on THIS LEG ONLY (#6522):
# the caller's fresh `string[]` argument temp is released at a counted-retain
# param position, and on wasm that closes the case — 197 B/round to flat. The
# x86-64 and arm64 legs still print "grows" and keep their rows, because the
# remaining leak is not in the shared lowering: `$__field_reclaim_<T>` here
# shallow-decs EVERY array-kind field, while `emit_ir_field_reclaim_one` /
# `emit_arm64_field_reclaim_one` release a `string[]` field only under the
# `strfldok:arr:<T>` admission, which this case's `fresh.deps[0].len()` element
# read withholds. The shared construction-side retain is unconditional either
# way, so those two legs keep one array reference per node. That asymmetry is
# the next slice, not a property of this leg.

# --- ACTIVE -----------------------------------------------------------------
# The reclaim shapes below were fixed in `internal/ir` — the NATIVE compiler —
# and the self-host compiler carries its own RC implementation, which has not
# been ported. So each case measures `flat` under native and `grows` here, on
# ALL THREE self-host targets identically: for these two the gap is in the
# shared self-host reclaim path rather than in any one emitter. That is a
# property of each row, not a rule — `alloc_flat_fresh_array_arg` above left
# this leg alone because the three field-reclaim emitters do NOT agree on a
# `string[]` field.
#
# Shape only: every ANSWER matches native and only the allocation class
# diverges, which is what these AL-01 cases exist to measure. A row that ever
# carries a VALUE divergence as well has to say so — the answer being wrong is
# a different order of bug from the memory being wrong.
#
# This is the RECLAIM half of goal 2 (docs/RC-PERCEUS-SELF-HOST-PORT.md §9),
# the same category as `alloc_flat_map_get` before #6875 closed it, and it is
# tracked as such. Each row goes when the corresponding rule is ported.
#
# Every row here must be a live divergence: a listed case that comes back
# matching native FAILS this leg, so an entry cannot outlive its bug.
alloc_flat_method_identity_return #6544 self-host prints "grows" where native prints "flat" on all three targets. Shape only — every answer matches, and all three legs diverge identically. The `.len()`-receiver rows are CLOSED, and so is the struct RECEIVER: a borrowing method no longer costs its receiver the deep drop, and an identity-returning one keeps it wherever the result is consumed inline. What still leaks is the chain's INTERMEDIATE view box (22 B/round, needs the outer callee proven borrowing and never-a-view) and the FRESH struct result of a method consumed by a field read (~95 B/round, `b.relabel("fresh-tag-value").tag.len()`). 117 B/round total. RECLAIM half of goal 2, docs/RC-PERCEUS-SELF-HOST-PORT.md §9.
