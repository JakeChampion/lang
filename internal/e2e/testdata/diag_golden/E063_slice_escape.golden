E063_slice_escape.fern:1:50: error[E063]: returning a `[T]` slice that views function-local storage: the backing array is reclaimed when "f" returns, leaving a dangling view — return an owned array (`T[]`) or slice a parameter instead
    function f(): [i32] { var xs: i32[] = [1, 2, 3]; return xs[0:2]; }
                                                     ^