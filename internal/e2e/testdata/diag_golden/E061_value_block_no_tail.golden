E061_value_block_no_tail.fern:1:43: error[E061]: block-expression has no trailing value (its last element is a `;`-terminated statement); a value is required here — drop the trailing `;` to make the final expression the block's value
    function main(): i32 { var x = if (1 < 2) { print("hi"); } else { 2 }; return 0; }
                                              ^
E061_value_block_no_tail.fern:1:32: error[E031]: if-expression branches differ: void vs i32
    function main(): i32 { var x = if (1 < 2) { print("hi"); } else { 2 }; return 0; }
                                   ^