// Unsigned `%` (#2904): (2^63 + 5) % (2^63) == 5 unsigned. Build the dividend
// by addition so it is a genuine bit-63-set u64 -> 5.
function main(): i32 { var one: u64 = 1 as u64; var big: u64 = one << 63; var n: u64 = big + (5 as u64); var r: u64 = n % big; return r as i32; }
