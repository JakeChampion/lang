// Unsigned `/` (#2904): 2^63 / 2 == 2^62 (unsigned). Signed idiv of the
// negative-looking 2^63 differs. Compared against 1<<62 -> 9.
function main(): i32 { var one: u64 = 1 as u64; var big: u64 = one << 63; var q: u64 = big / (2 as u64); if (q == (one << 62)) { return 9; } return 0; }
