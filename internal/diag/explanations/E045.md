# E045 — Map key / value type error

Three related errors on map literals:

- **Unsupported key type.** Only `i32` and `string` keys
  are wired today; other types reject.
- **Mixed key types.** Every entry's key must share the
  same type — first entry's type fixes the inferred map's
  key type, subsequent entries must match.
- **Mixed value types.** Same for values — first entry
  fixes the type, subsequent entries must match.

## Examples

```
function main(): i32 {
    var ok = map { 1: "a", 2: "b" };               // OK — Map[i32, string]
    var bad_k = map { 1: "a", "two": "b" };        // error[E045]: map key type string, expected i32
    var bad_v = map { 1: "a", 2: 42 };             // error[E045]: map value type i32, expected string
    var bad_kt = map { 1.5: "a" };                 // error[E045]: map key type f64 is not yet supported (use i32 or string)
    return len(ok);
}
```

## Fix

Align every entry's key and value types. Cast where the
underlying values can be converted.

```
function main(): i32 {
    var ok = map { 1: "a", 2: "b" };
    return len(ok);
}
```

## Why only i32 / string keys

The current Map runtime ([`internal/stdlib/core/map.fern`](../../internal/stdlib/core/map.fern))
specialises hash + comparison per key type; extending the
supported set is mechanical but unfinished. f32/f64 keys
are unlikely (NaN equality semantics are a separate
correctness landmine).

## See also

- E002 (type mismatch) — broader category.
