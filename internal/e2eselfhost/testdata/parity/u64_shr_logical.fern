// Logical `>>` (#2904): 2^63 >> 62 == 2 (unsigned); a signed >> sign-fills to a
// large value whose low bits differ. Returns 2.
function main(): i32 { var one: u64 = 1 as u64; var big: u64 = one << 63; var sh: u64 = big >> 62; return sh as i32; }
